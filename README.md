# Hackable spring-boot project

## Développement local

Une appli avec des failles de sécurité.

```
./mvnw spring-boot:run
```

## Déploiement Docker avec DB MySQL

```
docker compose up --build
```

Application disponible sur http://localhost:8080/
